
// IAM module (skeleton) — restored
// Provides placeholders for roles/service-principals to be implemented in Step 6.
resource "null_resource" "iam_placeholder" {
  triggers = { name = "iam-module-placeholder" }
}

